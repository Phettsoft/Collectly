<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:Collectly.Views.Pages"
       x:Class="Collectly.AppShell"
       FlyoutBehavior="Flyout"
       Title="Collectly">

    <Shell.FlyoutHeader>
        <Grid Padding="20" HeightRequest="120"
              BackgroundColor="{StaticResource Primary}">
            <HorizontalStackLayout VerticalOptions="Center" Spacing="12">
                <Image Source="appicon.png" HeightRequest="48" WidthRequest="48"/>
                <VerticalStackLayout VerticalOptions="Center" Spacing="2">
                    <Label Text="Collectly" FontSize="22" FontAttributes="Bold" TextColor="White"/>
                    <Label Text="Your collections, organized." FontSize="13" TextColor="White" Opacity="0.8"/>
                </VerticalStackLayout>
            </HorizontalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <FlyoutItem Title="Dashboard">
        <ShellContent ContentTemplate="{DataTemplate pages:DashboardPage}" Route="dashboard"/>
    </FlyoutItem>

    <FlyoutItem Title="Settings">
        <ShellContent ContentTemplate="{DataTemplate pages:SettingsPage}" Route="settings"/>
    </FlyoutItem>

    <FlyoutItem Title="About">
        <ShellContent ContentTemplate="{DataTemplate pages:AboutPage}" Route="about"/>
    </FlyoutItem>

</Shell>
